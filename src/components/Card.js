import React from 'react'

const Card = ({children}) => {
  return (
      <div>
    <div>Empieza una Card</div>
    {children}
    <div>Empieza una Card</div>
    </div>
    
  )
}

export default Card